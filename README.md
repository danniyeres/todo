# 📝 Task Management API

Это REST API для управления задачами с авторизацией через JWT. Вы можете создавать, просматривать, обновлять и удалять задачи, а также регистрировать и авторизовывать пользователей. Все защищённые эндпоинты требуют JWT-токен.

---

## 🚀 Технологии

- Java 17
- Spring Boot
- Spring Security + JWT
- Spring Data JPA + Hibernate
- PostgreSQL / H2
- Swagger / OpenAPI
- Lombok
- Maven
---

## 🔐 Функциональность

### Аутентификация
- **Регистрация пользователя**: `/auth/register` — создаёт нового пользователя.
- **Авторизация пользователя**: `/auth/login` — возвращает access и refresh токены.
- Получение access и refresh токенов
- **Защищённые эндпоинты**: все эндпоинты, кроме `/auth/*`, требуют JWT-токен.

### Задачи
- Создание задачи.
- Получение всех задач текущего пользователя (с фильтрацией по статусу: `TODO`, `IN_PROGRESS`, `DONE`).
- Получение задачи по ID.
- Обновление задачи.
- Удаление задачи.

---

## 🛠 Как запустить

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/danniyeres/todo.git
cd todo
```

### 3. Запустите проект
```bash
./mvnw spring-boot:run
```

Или:

```bash
mvn clean install
mvn spring-boot:run
```

После запуска приложение будет доступно по адресу `http://localhost:8080`.

---

## 📚 Swagger UI

Документация API доступна через Swagger UI. Откройте в браузере:

```
http://localhost:8080/swagger-ui/index.html
```

Для доступа к защищённым эндпоинтам нажмите кнопку "Authorize" в Swagger и вставьте токен в формате:

```
eyJhbGciOiJIUzI1NiIsInR5cCI6...
```

---

## 🧪 Эндпоинты API

### Аутентификация

#### POST /auth/register
Регистрирует нового пользователя.

**Запрос:**
```json
{
  "username": "user",
  "password": "123"
}
```

**Ответ (200 OK):**
```json
{
  "accessToken": "eyJhbGci...",
  "refreshToken": "eyJhbGci..."
}
```

**Ошибки:**
- `400 Bad Request` — некорректный формат запроса.
- `409 Conflict` — имя пользователя уже занято.

#### POST /auth/login
Авторизует пользователя и возвращает токены.

**Запрос:**
```json
{
  "username": "user",
  "password": "123"
}
```

**Ответ (200 OK):**
```json
{
  "accessToken": "eyJhbGci...",
  "refreshToken": "eyJhbGci..."
}
```

**Ошибки:**
- `401 Unauthorized` — неверные имя пользователя или пароль.
- `400 Bad Request` — некорректный формат запроса.

### Задачи

Все эндпоинты задач требуют заголовок `Authorization: Bearer <accessToken>`.

#### POST /tasks/create
Создаёт новую задачу для текущего пользователя.

**Запрос:**
```json
{
  "title": "Новая задача",
  "description": "Описание задачи",
  "status": "TODO"
}
```

**Ответ (200 OK):**
```json
{
  "id": 1,
  "title": "Новая задача",
  "description": "Описание задачи",
  "status": "TODO",
  "createdAt": "2025-06-08T22:57:00",
  "updatedAt": "2025-06-08T22:57:00"
}
```

**Ошибки:**
- `400 Bad Request` — некорректный формат запроса.
- `401 Unauthorized` — отсутствует или недействителен токен.

#### GET /tasks/get
Получает список задач текущего пользователя. Поддерживает фильтрацию по статусу через параметр `status`.

**Параметры запроса:**
- `status` (опционально): `TODO`, `IN_PROGRESS`, `DONE`.

**Пример запроса:**
```
GET /tasks/get?status=TODO
```

**Ответ (200 OK):**
```json
[
  {
    "id": 1,
    "title": "Новая задача",
    "description": "Описание задачи",
    "status": "TODO",
    "createdAt": "2025-06-08T22:57:00",
    "updatedAt": "2025-06-08T22:57:00"
  }
]
```

**Ошибки:**
- `401 Unauthorized` — отсутствует или недействителен токен.

#### GET /tasks/get/{id}
Получает задачу по её ID.

**Пример запроса:**
```
GET /tasks/get/1
```

**Ответ (200 OK):**
```json
{
  "id": 1,
  "title": "Новая задача",
  "description": "Описание задачи",
  "status": "TODO",
  "createdAt": "2025-06-08T22:57:00",
  "updatedAt": "2025-06-08T22:57:00"
}
```

**Ошибки:**
- `404 Not Found` — задача не найдена или не принадлежит пользователю.
- `401 Unauthorized` — отсутствует или недействителен токен.

#### PUT /tasks/update/{id}
Обновляет задачу по её ID.

**Запрос:**
```json
{
  "title": "Обновлённая задача",
  "description": "Новое описание",
  "status": "IN_PROGRESS"
}
```

**Ответ (200 OK):**
```json
{
  "id": 1,
  "title": "Обновлённая задача",
  "description": "Новое описание",
  "status": "IN_PROGRESS",
  "createdAt": "2025-06-08T22:57:00",
  "updatedAt": "2025-06-08T23:00:00"
}
```

**Ошибки:**
- `404 Not Found` — задача не найдена или не принадлежит пользователю.
- `400 Bad Request` — некорректный формат запроса.
- `401 Unauthorized` — отсутствует или недействителен токен.

#### DELETE /tasks/delete/{id}
Удаляет задачу по её ID.

**Пример запроса:**
```
DELETE /tasks/delete/1
```

**Ответ (204 No Content):**
Пустой ответ при успешном удалении.

**Ошибки:**
- `404 Not Found` — задача не найдена или не принадлежит пользователю.
- `401 Unauthorized` — отсутствует или недействителен токен.

---



## 🧪 Тестирование

1. Запустите приложение.
2. Откройте Swagger UI: `http://localhost:8080/swagger-ui/index.html`.
3. Зарегистрируйте пользователя через `/auth/register`.
4. Авторизуйтесь через `/auth/login` и получите токен.
5. Вставьте токен в Swagger (кнопка "Authorize").
6. Тестируйте эндпоинты задач.

---
---